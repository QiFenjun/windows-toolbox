# Windows 工具箱 v1.8.0

同时新增两个效率工具：

- **占用检测 / Lock Inspector**：Windows Restart Manager 单/多文件诊断，文件夹及驱动器 best-effort 扫描，批量查询、进度、取消和进程/服务信息。只诊断，不结束进程、不关闭句柄、不提权、不弹出设备。
- **保持唤醒 / Keep Awake**：系统或系统+显示器两种模式，15/30 分钟、1/2/4 小时及手动停止。专用工作线程持有临时请求，托盘继续运行，停止/到期/退出时释放。仅保存选择，不自动启用，不改变电源计划或模拟输入。

文件夹/驱动器结果不能覆盖仅占用目录、卷、设备或驱动程序句柄的情况；零结果不代表可安全拔出。长路径可能被 Restart Manager 拒绝，此时显示限制并继续其他文件。Keep Awake 不阻止锁屏、主动休眠或关键电源策略。

验证记录见仓库 `docs/v1.8.0-validation.md`。人工 UI 点击、真实 USB、多显示器/多 DPI 与电池/锁屏/睡眠硬件场景仍为 **Pending**；自动化视图加载与原生 API 临时夹具验证不等同人工验收。

下载 `WindowsToolbox-v1.8.0-win-x64.zip`，解压运行；SHA-256 见 `SHA256SUMS.txt`。
