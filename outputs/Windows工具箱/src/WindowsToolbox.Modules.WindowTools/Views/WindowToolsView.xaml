<UserControl x:Class="WindowsToolbox.Modules.WindowTools.Views.WindowToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="860">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Style="{DynamicResource CardStyle}" Padding="20">
                <DockPanel>
                    <StackPanel>
                        <TextBlock Text="窗口工具" Style="{DynamicResource SectionTitleStyle}"/>
                        <TextBlock Text="Window Tools" Style="{DynamicResource CaptionTextStyle}" Margin="0,2,0,0"/>
                        <TextBlock Text="置顶、定位、尺寸调整与信息查看；不读取窗口内容。" Style="{DynamicResource SecondaryTextStyle}" Margin="0,7,0,0"/>
                    </StackPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="选择前台窗口（1.5 秒）" Command="{Binding CaptureForegroundCommand}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="刷新窗口列表" Command="{Binding RefreshCommand}" Style="{DynamicResource PrimaryButtonStyle}"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="2" Style="{DynamicResource CardStyle}" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Left" Text="&#xE721;" Style="{DynamicResource FluentIconStyle}" Foreground="{DynamicResource SecondaryTextBrush}" Margin="6,0,10,0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource SearchTextBoxStyle}"
                             Height="38"
                             ToolTip="搜索窗口标题、进程名或 PID"/>
                </DockPanel>
            </Border>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" MinWidth="430"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="2*" MinWidth="320"/>
                </Grid.ColumnDefinitions>

                <Border Style="{DynamicResource CardStyle}" Padding="10">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="顶层窗口" Style="{DynamicResource SectionTitleStyle}" Margin="6,2,0,10"/>
                        <ListBox ItemsSource="{Binding WindowsView}" SelectedItem="{Binding SelectedWindow}"
                                 BorderThickness="0" Background="Transparent"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="8,7">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="42"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <Grid Width="34" Height="34" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FallbackGlyph}" Style="{DynamicResource FluentIconStyle}" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <Image Source="{Binding Icon}" Width="30" Height="30" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" Style="{DynamicResource CardTitleStyle}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding ProcessText}" Style="{DynamicResource CaptionTextStyle}" Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding SizeText}" Style="{DynamicResource CaptionTextStyle}" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding StateText}" Style="{DynamicResource CaptionTextStyle}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource CardStyle}" Padding="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <DockPanel>
                                <TextBlock Text="窗口详情" Style="{DynamicResource SectionTitleStyle}"/>
                                <Button DockPanel.Dock="Right" Content="复制信息" Click="CopyInfo_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
                            </DockPanel>
                            <TextBlock Text="{Binding SelectedWindow.Title, TargetNullValue=请选择左侧窗口}" Style="{DynamicResource CardTitleStyle}" Margin="0,14,0,0" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding SelectedWindow.ProcessText}" Style="{DynamicResource SecondaryTextStyle}" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.HwndText, StringFormat=HWND: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,6,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.ClassName, StringFormat=Class: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.ExecutablePath, StringFormat=路径: {0}, TargetNullValue=路径: 不可用}" Style="{DynamicResource CaptionTextStyle}" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedWindow.ExecutablePath}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.WindowRect, StringFormat=外框像素: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.ClientRect, StringFormat=客户区像素: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.MonitorText, StringFormat=显示器: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.DpiText}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.StateText, StringFormat=状态: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>
                            <TextBlock Text="{Binding SelectedWindow.TopMostText, StringFormat=置顶: {0}}" Style="{DynamicResource CaptionTextStyle}" Margin="0,3,0,0"/>

                            <Separator Margin="0,14,0,12"/>
                            <TextBlock Text="状态" Style="{DynamicResource FieldLabelStyle}"/>
                            <WrapPanel Margin="0,6,0,0">
                                <Button Content="置顶" Command="{Binding SetTopMostCommand}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                                <Button Content="取消置顶" Command="{Binding ClearTopMostCommand}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                            </WrapPanel>

                            <TextBlock Text="位置" Style="{DynamicResource FieldLabelStyle}" Margin="0,4,0,0"/>
                            <WrapPanel Margin="0,6,0,0">
                                <Button Content="居中" Command="{Binding CenterCommand}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                                <ComboBox ItemsSource="{Binding LayoutOptions}" SelectedItem="{Binding SelectedLayout}" DisplayMemberPath="DisplayName" Width="128" Style="{DynamicResource ModernComboBoxStyle}" Margin="0,0,8,8"/>
                                <Button Content="应用布局" Command="{Binding ApplyLayoutCommand}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                            </WrapPanel>

                            <TextBlock Text="尺寸（窗口外框物理像素）" Style="{DynamicResource FieldLabelStyle}" Margin="0,4,0,0"/>
                            <WrapPanel Margin="0,6,0,0">
                                <Button Content="1280 × 720" Command="{Binding ApplyPreset720Command}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                                <Button Content="1600 × 900" Command="{Binding ApplyPreset900Command}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                                <Button Content="1920 × 1080" Command="{Binding ApplyPreset1080Command}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                            </WrapPanel>
                            <WrapPanel Margin="0,0,0,8">
                                <TextBox Text="{Binding CustomWidth, UpdateSourceTrigger=PropertyChanged}" Width="80" Style="{DynamicResource ModernTextBoxStyle}" ToolTip="宽度" Margin="0,0,8,0"/>
                                <TextBox Text="{Binding CustomHeight, UpdateSourceTrigger=PropertyChanged}" Width="80" Style="{DynamicResource ModernTextBoxStyle}" ToolTip="高度" Margin="0,0,8,0"/>
                                <Button Content="应用自定义尺寸" Command="{Binding ApplyCustomSizeCommand}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}"/>
                            </WrapPanel>

                            <TextBlock Text="显示器" Style="{DynamicResource FieldLabelStyle}" Margin="0,4,0,0"/>
                            <WrapPanel Margin="0,6,0,0">
                                <ComboBox ItemsSource="{Binding Monitors}" SelectedItem="{Binding SelectedMonitor}" DisplayMemberPath="DisplayText" Width="220" Style="{DynamicResource ModernComboBoxStyle}" Margin="0,0,8,8"/>
                                <Button Content="移动并居中" Command="{Binding MoveMonitorCommand}" Style="{DynamicResource SecondaryButtonStyle}" IsEnabled="{Binding CanOperate}" Margin="0,0,8,8"/>
                            </WrapPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>

            <Border Grid.Row="6" Style="{DynamicResource EmptyStateCardStyle}" Padding="12">
                <StackPanel>
                    <TextBlock Text="{Binding Status}" Style="{DynamicResource SecondaryTextStyle}"/>
                    <TextBlock Text="{Binding Error}" Foreground="{DynamicResource DangerBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
